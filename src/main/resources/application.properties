#MONGODB
# Authentication database name.
spring.data.mongodb.authentication-database=
# Database name.
spring.data.mongodb.database=subaccounting
# Fully qualified name of the FieldNamingStrategy to use.
spring.data.mongodb.field-naming-strategy=
# GridFS database name.
spring.data.mongodb.grid-fs-database=
# Mongo server host. Cannot be set with URI.
spring.data.mongodb.host=127.0.0.1
# Login password of the mongo server. Cannot be set with URI.
spring.data.mongodb.password=
# Mongo server port. Cannot be set with URI.
spring.data.mongodb.port=8090
# Type of Mongo repositories to enable.
spring.data.mongodb.repositories.type=auto
# Mongo database URI. Cannot be set with host, port and credentials.
spring.data.mongodb.uri=mongodb://127.0.0.1
# Login user of the mongo server. Cannot be set with URI.
spring.data.mongodb.username=
# loccation of kafka server
kafka.bootstrapAddress = 127.0.0.1:9092

#the name of our Kafka topic
mongoRequestTopicName = mongoRequests

kafka.groupId = group1